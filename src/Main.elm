module Main exposing (main)

import Html exposing (div, h1, text)
import Html.Attributes exposing (..)


main =
    div
        []
        [ h1 [ class "font-mono text-8xl text-red-700" ] [ text "./onn.sh" ] ]
